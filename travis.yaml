language: node_js

node_js:
  - 10.16.3

cache:
  directories:
    - node_modules

branches:
  only:
    - master
    - feature/setup

script:
  - yarn verify
  - yarn build

after_success:
  - yarn release

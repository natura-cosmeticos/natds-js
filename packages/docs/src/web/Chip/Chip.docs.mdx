import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { Chip, Provider, themes } from '@naturacosmeticos/natds-web';
import { Delete } from '@material-ui/icons';
import { withTheme } from '../../../addons/theme/provider';
import ComponentWithLabel from '../../../addons/ComponentWithLabel';
import { attributes, variants, colors, sizes } from './sections';

# Chip

---
---

## Variants

<Provider theme={themes.natura.light}>
  <ComponentWithLabel componentList={variants} />
</Provider>

---
---

## Colors

<Provider theme={themes.natura.light}>
  <ComponentWithLabel componentList={colors} />
</Provider>

---
---

## Sizes

<Provider theme={themes.natura.light}>
  <ComponentWithLabel componentList={sizes} />
</Provider>

---
---

## Attributes

<Provider theme={themes.natura.light}>
  <ComponentWithLabel componentList={attributes} />
</Provider>

---
---

## API

```jsx highlight-line="2"
import { Chip } from '@naturacosmeticos/natds-web';

<Chip
  color="primary"
  variant="outlined"
  size="medium"
  label="Label"
/>
```

---
---

| Name                      | Type                                            | Default                       | Description
| :--                       | :-:                                             | :-:                           | :-:                       |
| avatar                    | element                                         |                               | Avatar element.
| children                  | unsupportedProp                                 |                               | This prop isn't supported. Use the component prop if you need to change the children structure.
| classes                   | object                                          |                               | Override or extend the styles applied to the component. See [Material Chip CSS API](https://material-ui.com/api/chip/#css) for more details.
| clickable                 | bool                                            |                               | If true, the chip will appear clickable, and will raise when pressed, even if the onClick prop is not defined. If false, the chip will not be clickable, even if onClick prop is defined. This can be used, for example, along with the component prop to indicate an anchor Chip is clickable.
| color                     | 'default', 'primary', 'secondary'               | 'default'                     | The color of the component. It supports those theme colors that make sense for this component.
| component                 | elementType                                     | 'div'                         | The component used for the root node. Either a string to use a DOM element or a component.
| deleteIcon                | element                                         |                               | Override the default delete icon element. Shown only if onDelete is set.
| disabled                  | bool                                            | false                         | If true, the chip should be displayed in a disabled state.
| icon                      | element                                         |                               | Icon element.
| label                     | node                                            |                               | The content of the label.
| onDelete                  | func                                            |                               | Callback function fired when the delete icon is clicked. If set, the delete icon will be shown.
| size                      | 'small', 'medium'                               | 'medium'                      | The size of the chip.
| variant                   | 'default', 'outlined'	                          | 'default'                     | The variant to use.

For more details consult the full [Material Chip API](https://material-ui.com/api/chip/)

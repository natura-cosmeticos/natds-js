import { Provider, themes } from '@naturacosmeticos/natds-web';
import ComponentWithLabel from '../../../addons/ComponentWithLabel';
import { attributes, directions } from './sections';

# Popover

---

---

## Attributes

<Provider theme={themes.natura.light}>
  <ComponentWithLabel componentList={attributes} />
</Provider>

---

---

## Directions

<Provider theme={themes.natura.light}>
  <ComponentWithLabel componentList={directions} itemsPerRow={3} />
</Provider>

---

---

## API

```jsx highlight-line="2"
import React from 'react';
import { Popover, Paper, Typography } from '@naturacosmeticos/natds-web';

const PopoverWrapper = () => {
  const anchorRef = React.useRef(null);

  const [open, setOpen] = React.useState(false);

  const handleClickButton = () => {
    setOpen(prevOpen => !prevOpen);
  };

  const direction = 'left';
  const actionLink = {
    text: 'Close Popover',
    onClick: () => alert('Action link was clicked.'),
  };

  return (
    <>
      <Button
        aria-describedby={direction}
        ref={anchorRef}
        variant="contained"
        color="primary"
      >
        Open Popover
      </Button>
      <Popover
        id={direction}
        open={open}
        direction={direction}
        anchorEl={anchorRef.current}
        actionLink={actionLink}
      >
        Lorem ipsum dolor
      </Popover>
    </>
  );
};
```

For more advanced usages, check [Material UI Popper docs](https://material-ui.com/pt/components/Popper/).

---

---

## Popover Props

| Name          | Type                                                                                                                              | Default      | Description                                                                                                                                  |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| anchorEl      |                                                                                                                                   | object, func | This is the reference element, or a function that returns the reference element, that may be used to set the position of the popove          |
| children      | node, func                                                                                                                        |              | Popper render function or node.                                                                                                              |
| container     | object, func                                                                                                                      |              | A node, component instance, or function that returns either.                                                                                 |
| disablePortal | bool                                                                                                                              | false        | Disable the portal behavior. The children stay within it's parent DOM hierarchy.                                                             |
| keepMounted   | bool                                                                                                                              | false        | Always keep the children in the DOM. This prop can be useful in SEO situation or when you want to maximize the responsiveness of the Popper. |
| open          | bool                                                                                                                              |              | If true, the popper is visible.                                                                                                              |
| placement     | 'bottom-end','bottom-start','bottom','left-end','left-start','left','right-end','right-start','right','top-end','top-start','top' | 'bottom'     | Popper placement.                                                                                                                            |
| popperOptions | object                                                                                                                            | {}           | Options provided to the popper.js instance.                                                                                                  |
| popperRef     | ref                                                                                                                               |              | A ref that points to the used popper instance.                                                                                               |
| transition    | bool                                                                                                                              | false        | Help supporting a react-transition-group/Transition component.                                                                               |

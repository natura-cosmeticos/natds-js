import { ICircularProgressProps } from "../CircularProgress.props";

export const colors : Record<ICircularProgressProps["color"], ICircularProgressProps["color"]> = {
  primary: "primary",
  secondary: "secondary",
  inherit: "inherit",
};

export default colors;

import { ButtonVariant } from "../Button.props";

export const variants: Record<ButtonVariant, ButtonVariant> = {
  contained: "contained",
  text: "text",
  outlined: "outlined",
};

export default variants;

import { ButtonColor } from "../Button.props";

export const colors : Record<ButtonColor, ButtonColor> = {
  default: "default",
  inherit: "inherit",
  primary: "primary",
  secondary: "secondary",
};

import { IconColor } from "../Icon.props";

export const colors : Record<IconColor, IconColor> = {
  action: "action",
  default: "default",
  disabled: "disabled",
  error: "error",
  inherit: "inherit",
  primary: "primary",
  secondary: "secondary",
};

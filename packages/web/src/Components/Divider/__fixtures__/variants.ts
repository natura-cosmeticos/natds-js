import { IDividerProps } from "../Divider.props";

export const variants: Record<IDividerProps["variant"], IDividerProps["variant"]> = {
  fullWidth: "fullWidth",
  inset: "inset",
  middle: "middle",
};

export default variants;

import { PropTypes } from "@material-ui/core";

export const colors: Record<PropTypes.Color, PropTypes.Color> = {
  default: "default",
  inherit: "inherit",
  primary: "primary",
  secondary: "secondary",
};

import { AppBarPosition } from "../AppBar.props";

export const positions: Record<AppBarPosition, AppBarPosition> = {
  absolute: "absolute",
  fixed: "fixed",
  relative: "relative",
  static: "static",
  sticky: "sticky",
};

import { ISwitchProps } from "../Switch.props";

export const colors: Record<ISwitchProps["color"], ISwitchProps["color"]> = {
  default: "default",
  primary: "primary",
  secondary: "secondary",
};

export default colors;

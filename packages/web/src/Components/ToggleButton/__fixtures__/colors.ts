import { IToggleButtonProps } from "../ToggleButton.props";

export const colors : Record<IToggleButtonProps["color"], IToggleButtonProps["color"]> = {
  primary: "primary",
  secondary: "secondary",
  default: "default",
  inherit: "inherit",
};

export default colors;

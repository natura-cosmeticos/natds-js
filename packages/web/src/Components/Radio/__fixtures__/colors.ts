import { IRadioProps } from "../Radio.props";

export const colors : Record<IRadioProps["color"], IRadioProps["color"]> = {
  default: "default",
  primary: "primary",
  secondary: "secondary",
};

export default colors;

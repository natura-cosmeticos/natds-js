import { ILinkProps } from "../Link.props";

export const colors : Record<ILinkProps["color"], ILinkProps["color"]> = {
  default: "default",
  initial: "initial",
  inherit: "inherit",
  error: "error",
  primary: "primary",
  secondary: "secondary",
  textPrimary: "textPrimary",
  textSecondary: "textSecondary",
};

export default colors;

import { PaperVariant } from "../Paper.props";

export const variants: Record<NonNullable<PaperVariant>, NonNullable<PaperVariant>> = {
  elevation: "elevation",
  outlined: "outlined",
};

export default variants;

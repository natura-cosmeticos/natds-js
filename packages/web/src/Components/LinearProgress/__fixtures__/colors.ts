import { ILinearProgressProps } from "../LinearProgress.props";

export const colors : Record<ILinearProgressProps["color"], ILinearProgressProps["color"]> = {
  primary: "primary",
  secondary: "secondary",
};

export default colors;

import { IDrawerProps } from "../Drawer.props";

export const anchors: Record<IDrawerProps["anchor"], IDrawerProps["anchor"]> = {
  left: "left",
  top: "top",
  right: "right",
  bottom: "bottom",
};

export default anchors;
